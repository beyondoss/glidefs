target
/mnt
/cache
bench/bench_workspace
glidefs.toml
TODO.md
.claude/settings.local.json
glidefs/fuzz/corpus
glidefs/fuzz/artifacts
# fio verify state files left behind by `cargo test --test fio_verify`.
glidefs/*-verify.state
local-*-verify.state
.claude/scheduled_tasks.lock

# Boot-set profiling initramfs (regenerated by build.rs into OUT_DIR; build.sh is a dev tool)
glidefs/src/oci/sandbox/vm_init/*.cpio

# Boot-set profiling artifacts (binary block lists, regenerated by `glidefs profile`)
*.bootset
*.bootset.L*
